{
  "name": "mpt-sst",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "build": "sst build",
    "deploy-dev": "export LOCAL_ENV=dev && npm run build --prefix ./packages/nuxt && npx sst deploy --stage dev",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "deploy-prod": "export LOCAL_ENV=prod && npm run build --prefix ./packages/nuxt && npx sst deploy --stage prod"
  },
  "devDependencies": {
    "sst": "2.38.7",
    "aws-cdk-lib": "2.110.1",
    "constructs": "10.3.0",
    "typescript": "^5.0.3",
    "@tsconfig/node16": "^1.0.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
