{
  "name": "mpt-sst",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "web": "cd packages/web && npm run dev",
    "build": "sst build",
    "deploy-dev": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build --prefix ./packages/nuxt && npx sst deploy --stage prod"
  },
  "devDependencies": {
    "sst": "^2.4.0",
    "aws-cdk-lib": "2.62.2",
    "constructs": "10.1.156",
    "typescript": "^5.0.3",
    "@tsconfig/node16": "^1.0.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
